#include <iostream>
#include <Avion.h>
#include <Boing.h>
#include <Torres.h>
#include <vector>
using namespace std;

int main()
{

    Boing q,w,e,r,t,y,u,i,o,p;
    Torres xd;
    xd.agregar(q);
    xd.agregar(w);
    xd.agregar(e);
    xd.agregar(r);
    xd.agregar(t);
    xd.agregar(y);
    xd.agregar(u);
    xd.agregar(i);
    xd.agregar(o);
    xd.agregar(arr);
    xd.agregar(arr);

    xd.recorrer();
    return 0;
}
//https://www.youtube.com/watch?v=yVUUSXwjeDs :3333333333
